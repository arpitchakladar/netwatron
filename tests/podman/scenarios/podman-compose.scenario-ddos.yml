# Override: run DDoS scenario (distributed across all attackers)
# Usage: podman-compose -f docker-compose.yml -f podman-compose.scenario-ddos.yml up

version: "3.8"

services:
  attacker1:
    environment:
      - SCENARIO=ddos_distributed
    command: sh -c 'python /app/tests/ddos_distributed.py'

  attacker2:
    environment:
      - SCENARIO=ddos_distributed
    command: sh -c 'python /app/tests/ddos_distributed.py'

  attacker3:
    environment:
      - SCENARIO=ddos_distributed
    command: sh -c 'python /app/tests/ddos_distributed.py'

  attacker4:
    environment:
      - SCENARIO=ddos_distributed
    command: sh -c 'python /app/tests/ddos_distributed.py'

  attacker5:
    environment:
      - SCENARIO=ddos_distributed
    command: sh -c 'python /app/tests/ddos_distributed.py'
